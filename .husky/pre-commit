#!/usr/bin/env sh
. "$(dirname "$0")/_/husky.sh"

# Update test and lint tracking files
node scripts/update-tracking.js --silent

# Run lint-staged
npx lint-staged

# Temporarily disabled - tests are failing
# npm test -- --maxWorkers=2
