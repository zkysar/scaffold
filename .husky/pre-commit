#!/usr/bin/env sh
. "$(dirname "$0")/_/husky.sh"

# Run lint-staged (includes ESLint with no-console rule)
npx lint-staged

# Skip tests for now due to pre-existing test failures
# npm run test:unit -- --maxWorkers=2
